You are a structured dataset generator. Generate JSON flashcards as NDJSON (one JSON object per line, no extra text). Produce exactly 75 flashcards. Each JSON object MUST conform to the v1.0 flashcard schema provided to you: input, output, meta.

Constraints / fixed fields:
- input.goals = ["asset_protection", "confidentiality"]
- output.recommended_structures = ["bvi_trust"]
- meta.subsection = "intl_offshore_bvi_trust"
- meta.difficulty = "complex"
- meta.source_prompt_id = "P1_IOBT_01"
- meta.version = "1.0"

Diversity seeds:
- Jurisdictions (use a balanced mix): ["hk", "sg", "ch", "pa"]
- Industries (use for inspiration): [family_office, high_value_art, cryptocurrency_portfolio, international_real_estate, inheritance_planning]

For each card:
- IMPORTANT: For every single flashcard, you must include all the fields listed in the "Constraints / fixed fields" section with their specified values.
- Write a realistic short scenario in input.scenario (one to two sentences) for a high-net-worth individual or family office from one of the jurisdictions.
- The scenario should emphasize the need for privacy, protection from creditors, or complex international estate planning.
- Populate input.jurisdiction with the chosen jurisdiction for the scenario.
- Populate input.assumptions and input.constraints with realistic, 1-sentence details based on the scenario.
- Provide meta.rationale: 1–3 sentences explaining why a BVI Trust is recommended for high confidentiality and asset protection needs.
- Set output.policy_refs to a list containing a single internal tag, like ["internal_policy:bvi_trust_structuring_v1"].

If a jurisdiction outside the allowed set appears, emit {"error":"invalid_jurisdiction","value":"<value>"}.
