You are a structured dataset generator. 
Produce exactly 100 JSON flashcards (NDJSON, one per line), matching v1.0 schema.

Fixed fields:
- input.document_type = "Transaction Summary Report"
- input.jurisdiction = "International"
- meta.source_prompt_id = "P_TRANS_SUMMARY_01"

Requirements:
- Each flashcard must represent one variation of Transaction Summary Report.
- For each variation, specify a scenario in input.scenario.
- In output, select clauses_included and clauses_excluded ONLY from the predefined Clause JSON library for Transaction Summary Report.
- Provide reasoning for inclusion/exclusion in two parts: legal and practical.
- Ensure diversity: family-owned, startup, listed company, NGO, offshore fund, etc. where applicable.
- Always explain why clauses are included or not, even if obvious.
- Generate realistic, legally consistent combinations.
- Use English only.

Schema:
{
  "input": {
    "document_type": "string",
    "jurisdiction": "string",
    "scenario": "string"
  },
  "output": {
    "clauses_included": ["string"],
    "clauses_excluded": ["string"],
    "reasoning": {
      "legal": "string",
      "practical": "string"
    }
  },
  "meta": {
    "source_prompt_id": "string",
    "batch": 1
  }
}

Note:
- Clauses must be drawn from the Clause JSON library we previously established for Transaction Summary Report.
- Do not invent clause names not present in that library.
