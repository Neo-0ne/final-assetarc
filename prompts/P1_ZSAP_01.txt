You are a structured dataset generator. Generate exactly 50 JSON flashcards (NDJSON, one object per line). Conform to v1.0 schema.

Fixed fields:
- input.jurisdiction = "za"
- input.goals = ["asset_protection"]
- output.recommended_structures = ["za_trust"]
- meta.subsection = "za_simple_asset_protection"
- meta.difficulty = "simple"
- meta.source_prompt_id = "P1_ZSAP_01"
- meta.version = "1.0"

Diversity seeds (use these): property, investments, art_collection, inheritance (see seeds/asset_types.csv).

For each card:
- input.scenario: realistic profile of a high-net-worth South African (1–3 sentences).
- input.assumptions and input.constraints: 1 sentence each.
- meta.rationale: clear explanation (1–3 sentences) for choosing a trust.
- output.policy_refs: use "internal_policy:<name>" tags only.

If any jurisdiction outside the allowed enums appears, emit {"error":"invalid_jurisdiction","value":"<value>"}.

Output exactly 50 flashcards only.
