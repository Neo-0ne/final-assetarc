You are an expert in business succession planning. Your task is to generate a high-quality dataset for training an AI model that provides succession planning recommendations.

**Primary Objective:**
Generate exactly 30 flashcards in NDJSON format. Each flashcard must represent a different scenario for the succession planner.

**Flashcard Schema:**
Each JSON object must have three top-level keys: `input`, `output`, and `meta`.

**1. Input Schema:**
The `input` object must have two boolean keys:
```json
{
  "has_partners": "boolean",
  "has_buy_sell": "boolean"
}
```

**2. Output Schema:**
The `output` object must represent the recommendations generated by the planner. The `details.recommendations` array must contain the exact `id`, `title`, and `text` values returned by the `run_succession_check` function.

**3. Meta Schema:**
```json
{
  "rationale": "string",
  "source_prompt_id": "P2_SUCCESSION_PLAN_01"
}
```

**Content Generation Constraints (Very Important):**
1.  **Cover all Scenarios:** You must generate flashcards for all three logical branches of the `run_succession_check` function.
    *   **Scenario A (10 flashcards):** `has_partners: true`, `has_buy_sell: false`. The output must contain recommendations REC001 and REC002.
    *   **Scenario B (10 flashcards):** `has_partners: true`, `has_buy_sell: true`. The output must contain recommendation REC003.
    *   **Scenario C (10 flashcards):** `has_partners: false`. The `has_buy_sell` value is irrelevant here. The output must contain recommendation REC004.
2.  **Logical Consistency:** The `output` must be the *exact* correct result for the given `input`.
3.  **Rationale:** The `meta.rationale` must briefly explain why the input combination leads to the specific output recommendations. For example: "The user has partners but no buy-sell agreement, which presents a significant business risk. Therefore, the primary recommendations are to create this critical agreement and to fund it with key person insurance."

**Example Flashcard (Scenario A):**
{"input": {"has_partners": true, "has_buy_sell": false}, "output": {"status": "completed", "summary": "Found 2 succession planning recommendations.", "details": {"recommendations": [{"id": "REC001", "title": "Create a Buy-Sell Agreement", "text": "You have business partners but no Buy-Sell Agreement. This is a significant risk. A Buy-Sell agreement ensures a smooth transition of ownership and prevents disputes if a partner exits."}, {"id": "REC002", "title": "Consider Key Person Insurance", "text": "Key Person Insurance can provide the necessary funds for the remaining partners to buy out a departing partner's shares, preventing a liquidity crisis."}]}}, "meta": {"rationale": "The combination of having partners without a buy-sell agreement triggers the recommendations to establish one and fund it appropriately. This addresses the core risk of partner exit.", "source_prompt_id": "P2_SUCCESSION_PLAN_01"}}

Now, please generate exactly 30 flashcards following these instructions.
